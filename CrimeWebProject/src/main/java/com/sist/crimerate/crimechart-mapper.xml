<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.crimerate.crimechart-mapper">
  <select id="totalChartData" resultType="crmVO" parameterType="java.util.Map">
  	SELECT c_gu,c_year,sum(c_count) as total ,round(sum(c_count)/5,2) as average
	FROM crimerate
	<where>
		<if test="guList.size()!=0 and seoulgu!=null">
         	c_gu IN
         	<foreach collection="guList" item="sg" open="(" close=")" separator=",">
         		#{sg}
         	</foreach>
         </if>
	</where>
	GROUP BY c_gu,c_year
	ORDER BY c_gu,c_year;
  </select>
  
<!--   <select id="chartListData" resultType="crmVO" parameterType="java.util.Map">
  	SELECT  c_year, c_gu,c_name, c_count, c_type
	FROM (SELECT  c_year, c_gu,c_name, c_count, c_type 
	FROM crimerate 
	<where>
		<if test="guList.size()!=0 and seoulgu!=null">
         	c_gu IN
         	<foreach collection="guList" item="sg" open="(" close=")" separator=",">
         		#{sg}
         	</foreach>
         </if>
	</where>
	)WHERE c_type='발생' order by c_year, c_gu;
  </select> -->
</mapper>
